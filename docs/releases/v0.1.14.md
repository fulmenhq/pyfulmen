# Release v0.1.14 - 2025-11-29

**Release Type**: Critical Packaging Fix
**Release Date**: November 29, 2025
**Status**: ✅ Ready for Release

**Summary**: PyFulmen v0.1.14 resolves a critical packaging issue where `src/crucible` assets (schemas, config defaults, generated exit codes) were excluded from the wheel distribution. This caused `ModuleNotFoundError: No module named 'crucible'` when importing `pyfulmen` in fresh environments.

## 🐛 Fixes

- **Wheel Packaging**: Added missing `src/crucible` package to wheel distribution in `pyproject.toml`.
- **Import Safety**: Verified availability of generated assets (exit codes, schemas, config defaults) via new integration test `tests/integration/crucible/test_consumer_usage.py`.

## 📦 Distribution

This release ensures that `pip install pyfulmen` installs both:

- `pyfulmen` (library code)
- `crucible` (synced assets and generated code)

## ✅ Quality Gates

- [x] **Tests**: Added consumer usage test case verifying `crucible` module import and bridge API access.
- [x] **Verification**: Confirmed wheel contents include `src/crucible` directory structure.
